<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ui_prototype"
        tools:targetApi="31">

        <!-- Set SignInActivity as the launcher activity -->
        <activity
            android:name=".SignInActivity"
            android:exported="true"
            android:label="@string/title_activity_sign_in"
            android:theme="@style/Theme.Ui_prototype">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- SignUpActivity configuration -->
        <activity
            android:name=".SignUpActivity"
            android:exported="true" />

        <!-- MainActivity configuration without the LAUNCHER intent-filter -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <!-- Google Maps API key meta-data remains unchanged -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />

        <!-- Add other activity declarations and meta-data tags as needed -->

    </application>

</manifest>
